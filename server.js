const express = require('express'); const cors = require('cors'); const app = express(); app.use(cors()); app.use(express.json()); app.post('/moderate',(req,res)=>{ const text=(req.body&&req.body.text)||''; const kw=['suicid','matar','me quiero morir','no quiero vivir','lastim']; const risk = kw.some(k=> text.toLowerCase().includes(k)); res.json({ safe: !risk, risk }); }); const PORT = process.env.PORT||4000; app.listen(PORT, ()=>console.log('Moderation backend listening on', PORT));